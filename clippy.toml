disallowed-types = [
    { path = "std::collections::HashMap", reason = "core iteration must be ordered; use BTreeMap or Vec" },
    { path = "std::collections::HashSet", reason = "core iteration must be ordered; use BTreeSet or Vec" },
    { path = "std::time::SystemTime", reason = "ambient time is forbidden in deterministic code" },
    { path = "std::time::Instant", reason = "ambient time is forbidden in deterministic code" },
]

