[package]
name = "cc-kv"
version.workspace = true
edition.workspace = true
description = "Replicated key/value state machine with log-time TTL and sessions."
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
cc-core = { path = "../cc-core" }
cc-store = { path = "../cc-store" }

[lints]
workspace = true

