[package]
name = "cc-checker"
version.workspace = true
edition.workspace = true
description = "History, linearizability, and lightweight trace invariant checking."
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
cc-core = { path = "../cc-core" }
cc-cluster = { path = "../cc-cluster" }

[lints]
workspace = true

