[package]
name = "cc-log"
version.workspace = true
edition.workspace = true
description = "One durable Raft-log value layer over checksummed WAL records."
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
cc-core = { path = "../cc-core" }
cc-env = { path = "../cc-env" }
cc-raft = { path = "../cc-raft" }
cc-wal = { path = "../cc-wal" }

[lints]
workspace = true
