[package]
name = "cc-cluster"
version.workspace = true
edition.workspace = true
description = "The composition boundary: one Raft node, one KV state machine, value-only effects."
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
cc-core = { path = "../cc-core" }
cc-env = { path = "../cc-env" }
cc-kv = { path = "../cc-kv" }
cc-raft = { path = "../cc-raft" }
cc-store = { path = "../cc-store" }
cc-wal = { path = "../cc-wal" }

[lints]
workspace = true

