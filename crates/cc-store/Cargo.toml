[package]
name = "cc-store"
version.workspace = true
edition.workspace = true
description = "A deterministic, single-keyspace LSM store built on the Crash Course WAL."
license.workspace = true
authors.workspace = true
publish = false

[features]
default = []
kata04 = []

[dependencies]
cc-core = { path = "../cc-core" }
cc-env = { path = "../cc-env" }
cc-wal = { path = "../cc-wal" }

[lints]
workspace = true
