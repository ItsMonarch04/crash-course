[package]
name = "cc-sim"
version.workspace = true
edition.workspace = true
description = "A small deterministic discrete-event host used by tests and cluster work."
license.workspace = true
authors.workspace = true
publish = false

[dependencies]
cc-core = { path = "../cc-core" }
cc-env = { path = "../cc-env" }
cc-raft = { path = "../cc-raft" }

[lints]
workspace = true
