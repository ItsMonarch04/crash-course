# Museum

The museum records only failures found by the deterministic harness. Each real
exhibit will pin a build, seed, profile, minimized fault plan, verdict, and the
fix. An empty catalogue is preferable to a fabricated story.

